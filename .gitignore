.venv/
__pycache__/
*.pyc
.pytest_cache/
.env

# Local/generated database files - the real dev database is Postgres via
# docker-compose.yml; any *.db file here is a stray SQLite artifact
# (e.g. from the test suite's per-session sqlite files, or an ad-hoc
# local run) and must never be committed.
*.db
*.sqlite3

# OS/editor cruft
.DS_Store

# Local Claude Code tooling config/state - not part of the project itself.
.claude/
